<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Home Fragment Interest-->
    <style name="card_interest" parent="Widget.MaterialComponents.CardView">
        <item name="android:backgroundTint">@color/bottom_nav_bar</item>
    </style>

    <!-- Daily Activity Fragment-->
    <style name="cardFriend" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_marginStart">@dimen/dimen10</item>
        <item name="android:layout_marginEnd">@dimen/dimen10</item>
        <item name="cardCornerRadius">@dimen/dimen20</item>
        <item name="cardElevation">@dimen/dimen10</item>
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <!-- Galery Fragment -->
    <style name="card_gallery" parent="Widget.MaterialComponents.CardView">
        <item name="android:backgroundTint">#00000000</item>
    </style>

</resources>